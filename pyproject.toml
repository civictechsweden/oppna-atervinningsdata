[project]
name = "oppna-atervinningsdata"
version = "0.1.0"
description = "Automatically fetch all Swedish recycling stations using Avfall Sverige's open APIs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32.3",
    "requests-futures>=1.0.1",
]

[tool.uv]
package = false
